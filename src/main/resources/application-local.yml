spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gym
    username: gym
    password: gym
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.gym.crm: DEBUG
    org.hibernate.SQL: DEBUG